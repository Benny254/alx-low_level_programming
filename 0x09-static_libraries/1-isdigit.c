#include "main.h"

/**
 * _isdigit - to check for a digit(0 to 9)
 * @c: the int to be checked
 * Return: check 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
