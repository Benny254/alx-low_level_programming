#include "main.h"

/**
 * _islower - to check for lowercase character
 * @c: the character to be checked
 * Return: 1 if c is lowercase, the  0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
