#include "main.h"

/**
 * _abs - to compute the absolute value of an integer
 * @n: the int to be checked
 * Return: the absolute value of int to be checked
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
